写时复制
1、什么是CopyOnWrite容器
当我们往容器添加元素的时候，不直接往当前容器添加，而是先将当前容器copy,复制出一个新的容器，然后往新的容器添加元素。
再将原容器的引用指向新的容器。
也可以理解为一种读写分离的思想，读和写不同的容器。

2、CopyOnWriteArrayList的实现原理
在add的时候是需要加锁的，否则多线程会copy出N多个副本
读的时候不会加锁，因为不会锁住旧的
3、CopyOnWrite使用场景
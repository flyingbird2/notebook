1、next-key lock 是间隙锁加行锁的效果，是前开后闭的区间
间隙锁与间隙锁并不是互斥的，间隙锁就是对一个间隙进行加锁，2个事务可以对同一个间隙都加间隙锁，但是他们与对这一间隙的数据变更操作互斥，比如insert、update等。但是next-key lock和间隙锁不一样，它包含行锁，对于同一条记录，2个行锁是互斥的，所以会出现一个next-key lock等待另一个next-key lock释放锁的现象。
